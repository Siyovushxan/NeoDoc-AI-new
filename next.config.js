/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  // 'eslint' qismini olib tashladik, chunki bu versiyada u alohida boshqariladi
};

module.exports = nextConfig;